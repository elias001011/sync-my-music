# esbuild ships its Windows binary via an optional platform package, so its
# build script is a no-op here — approve it to keep `pnpm install` exit-clean.
# Skip the pre-run deps check so `pnpm build:assets` runs without a reinstall.
verify-deps-before-run=false
