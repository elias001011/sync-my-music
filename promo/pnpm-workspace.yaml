# esbuild ships its Windows binary via an optional platform package, so its
# build script is effectively a no-op here; approve it to keep installs clean.
allowBuilds:
  esbuild: true

overrides:
  fast-uri: ^3.1.5
  nanoid: ^3.3.18
  postcss: ^8.5.23
