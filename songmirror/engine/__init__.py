"""Engine — the provider-agnostic sync core: matching, reconcile/mirror, the
provider protocol (targets/), the SQLite archive, and per-service clients. Knows
nothing about the web or services tiers."""
