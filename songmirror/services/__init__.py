"""Services — the platform layer over the engine: settings store, event bus,
sync scheduler, account connectors, playlist pairing, and transfers. Depends on
the engine; never imports the web tier."""
